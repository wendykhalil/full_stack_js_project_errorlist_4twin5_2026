apiVersion: apps/v1
kind: Deployment
metadata:
  name: bmp-frontend
  namespace: bmp-production
  labels:
    app: bmp-frontend
    tier: frontend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: bmp-frontend
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: bmp-frontend
        tier: frontend
    spec:
      containers:
        - name: frontend
          image: bmptn/bmp-frontend:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 80
              name: http
          resources:
            requests:
              memory: "64Mi"
              cpu: "100m"
            limits:
              memory: "128Mi"
              cpu: "200m"
          livenessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 10
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 10
      restartPolicy: Always
